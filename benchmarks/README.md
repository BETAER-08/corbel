# Benchmarks

This directory holds the measurement harness for corbel.

`benchmarks/repos/` and `benchmarks/results/` are ignored by git. The harness
clones external repositories into `benchmarks/repos/` and writes measurements to
`benchmarks/results/`. Never commit the contents of either directory.
